# Minimal requirements for Docker containers (prediction-optimized)
# Approximately 500MB vs 2GB for full requirements.txt

# Core ML
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
joblib>=1.3.0

# Data formats
polars>=0.19.0
pyarrow>=13.0.0
pyyaml>=6.0

# Astronomy (minimal)
astropy>=5.3.0

# Utilities
tqdm>=4.66.0
requests>=2.31.0
huggingface_hub>=0.19.0

# Optional: Uncomment for validation plots
# matplotlib>=3.8.0
# seaborn>=0.12.0
